---
interface Props {
  title: string
  subtitle: string
  description: string
  thumb: string
  useCesium: boolean
}

const baseUrl = import.meta.env.BASE_URL
const { title, subtitle, description, thumb, useCesium } = Astro.props
const thumbUrl = `https://humo-tech.github.io${baseUrl}/${thumb}`
---

<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
  </head>
  <body>
    <slot />
  </body>
</html>
<style is:global>
  html {
    font-family: system-ui, sans-serif;
    background: #13151a;
    background-size: 224px;
    width: 100%;
    height: 100%;
  }
  body {
    margin: 0;
    width: 100%;
    height: 100%;
  }
</style>
